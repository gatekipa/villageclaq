# Production S0/M2 schema-only qualification branch — bounded result

Owner: Daybreak Blue. Candidate code: `cb5581dff8bb7b9a9b990ede5509bff7d246f21b`; starting evidence tip: `bd99665be80e2620fd3457a86d6e072c45e8a861`. Branch: `codex/qualification-repair-247c8881`. This is **HOLD**, not release or production qualification.

## Authorized isolation and preservation

- Source `llbnliixczcqfftxpsmb` was read only. A PostgreSQL 17 `pg_dump --schema-only --schema=public` yielded 685,672 bytes, SHA-256 `3b539557f807efca02b92fde9307228c45a6b398abc60a96e000150b1d1bdd4a`. It contains DDL, no table rows, credentials, or secrets. The local dump remains ignored under `.vercel/`; only this manifest is published.
- Target was confirmed as the existing billed branch `nisipxbuvndobyxqqglf` (branch ID `2799efdd-2a84-46b6-9cc4-ec8709e0ce39`) under that project. Before its authorized reset, the branch-only schema dump was saved locally with SHA-256 `6a7939fc2f4f49df23ff5284ba38ac9126398b613b9bacb23f7bf7c0e1146b18`; fictional public/financial data were saved with SHA-256 `54caeafb7c2f5c142a58100c18622ffc0a22dc4c2c6ba627f77cc52a859d06cb`. Existing tracked VC-01/02 evidence remains accessible. No production data were copied.
- The Supabase branch reset reported success but retained 77 partial public tables, `financial_core`, `financial_private`, and 47 synthetic `qualification_*` history rows while clearing fictional auth/application rows. An isolated-branch, single-transaction import replaced the partial public/financial_core schemas, removed synthetic history, and restored the `auth.users` application trigger. `financial_private` residue was then removed before 00118. The branch's actual baseline had 92 public tables, `election_ballots`, no F3 ledger, and zero synthetic history rows.
- Import adaptation: the temporary `postgres` login could not alter the 12 **`supabase_admin`-owned default-privilege grants** in the production dump. The first attempt rolled back. The completed import omitted only those statements; all explicit table/function grants and policies were retained. Branch-scoped default privileges therefore differ for objects later created by `supabase_admin`. No application permission was broadened to make the import pass. The first import copy also changed embedded function line endings; it was replaced with a byte-preserving filtered copy. The final branch `has_group_permission` definition/source MD5 exactly match production and 00118's pins: `695368464e97297fbf0f90ce7345162f` / `96a296dfd541c7fc75ec68c4da1d92ff`.

## Incremental upgrade result

Repository files `00118`–`00121` applied in order, one transaction per file. Unchanged `00122` initially stopped because it calls `public.uuid_generate_v5(uuid,text)`, while production and the branch expose only `extensions.uuid_generate_v5(uuid,text)`. A temporary one-line direct-extension diagnostic allowed `00122`–`00127` to apply. The source was then restored byte-for-byte; the branch received the thin public wrapper already used by the F24 fixture, and its `f3_correction_child_id` function was replaced with the original source body. A deterministic UUID comparison passed. This demonstrates the missing prerequisite but **does not qualify an exact-file `00122` replay on the production baseline**. The wrapper is a fixture adaptation, not an installed production prerequisite. A production deploy path must explicitly supply it or qualify a revised 00122 and its affected file-based runner.

`00128_m7_01_relief_canonical.sql` failed atomically at its preflight: `M7_ABORT: financial_core.currencies missing`. The installed canonical F3 has no such relation. Source inspection of 00128–00131 also found calls to a nonexistent one-argument `financial_core.post_f3_command(jsonb)`; actual F3 exposes `(jsonb,jsonb)`. The 00128 Relief command passes a UUID as its second argument and constructs a different command shape; 00129 uses nonexistent `financial_core.accounts`. These are **demonstrated source/catalog contract mismatches**, separate from the lost six-edit F3 fragment. An adapter or removal of the preflight alone would not establish correct financial effects. `00128`–`00137` were not applied on this production-baseline branch. Current branch history is zero because direct `psql` imports do not record Supabase migration history; this is a schema-order diagnostic, not a successful `db push`/history qualification.

The previous F24 result still proves `00118`–`00123` runner mechanics on its documented stub/live-pin fixture. The former representative 00137 branch/browser result still proves the focused VC-01/02 behavior on its stated partial schema. Neither result proves this currently blocked production-baseline chain. No new finance or browser write was attempted after the branch reset because the candidate schema cannot yet reach 00137.

## Concrete continuation

1. Daybreak Blue: design and test a production-compatible 00122 UUID prerequisite without changing frozen F24 claims; reconcile 00128–00131 with canonical F3 schema and command semantics, including Relief/loan/ticket/statement financial effects and atomic evidence. Then repair exact 00134–00136 membership, permission, token, and queue contracts without weakening grants; apply 00137 and verify migration history/ACLs on this same branch.
2. Daybreak Blue: reseed only fictional actors with external messaging suppressed, verify the candidate points exclusively to `nisipxbuvndobyxqqglf`, and run the remaining VC-04 database/browser matrix. Changed schema requires focused VC-01/02 integration recheck at 00137, while their earlier tested-scope closure remains intact.
3. Jude: decide the scope and dependencies for missing canonical Hierarchy 2.0 and Elections 2.0, plus any other mandatory product work that exceeds focused compatibility. Independent Astra access or an explicitly approved substitute remains a separate decision.

The existing branch cost is the previously approved `$0.01344/hour`. Daybreak Blue retains it through bounded qualification and independent review, then coordinates cleanup when evidence no longer needs it. No additional paid environment, production write, main merge, or deployment occurred.
