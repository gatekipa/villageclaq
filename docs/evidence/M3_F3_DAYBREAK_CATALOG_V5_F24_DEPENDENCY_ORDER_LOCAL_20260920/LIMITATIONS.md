# Evidentiary limits

DOCUMENTED QUALIFICATION FIXTURE — NOT A CLEAN 00001–00117 REPLAY AND NOT PRODUCTION-EQUIVALENT

- Local this-run PostgreSQL 17.11 only. Not hosted identity proof. Disposable `jkorwnwwmdeflfntxntl` and production `llbnliixczcqfftxpsmb` were not contacted.
- Fixture installs pinned 00117 owned-policy/trigger/CHECK SQL plus a 00119-style trigger/policy/`LANGUAGE sql` `can_manage` slice. It is **not** a clean 00001–00117 replay and is **not** production-equivalent.
- Old-order reproduction is the demonstrated failing statement (`DROP FUNCTION public.has_group_permission(uuid,text,uuid) RESTRICT`) against that fixture. It is **not** a byte-replay of the entire pre-correction generated reset script from `66be2b47`. The DETAIL/HINT shape matches ATTEMPT_2 (`m2_np_*`, CASCADE named and unused).
- Corrected path is the actual shared reset (`executeSharedReset` / generated allowlist SQL). Applied SQL identity `ece1a3f2cfa912ae35dbb8b82baeaf61af935e0fb0adce8dd8caee9f7eb50080`.
- Unsupported-dependent durable check is a subsequent `psql` connection to the same local disposable database after eligibility rejected mutation. Leftover table, leftover policy, notification objects, and history count 6 remained.
- Unchanged F15–F23 transaction, lock-wait, wipe, target-guard, and fingerprint behaviors were not rerun. Reuse accepted evidence.
- ATTEMPT_1 and ATTEMPT_2 hosted packages were not rewritten. Their plan SHA `bf9ca7b6…` and old closures remain the consumed-budget record.
- No third hosted attempt. Local profile `LOCAL_FINGERPRINT_COMPARISON_PROFILE_V1` still grants no hosted authority.
- Astra: not contacted. Daybreak: not contacted.
