# R-011 platform aggregate boundary — 2026-09-26

**Executor:** Daybreak Blue. **Tested code:** `157e9ebf0b9425797d6a3e9802814ab515312161`. **Environment:** authorized isolated Supabase branch `nisipxbuvndobyxqqglf`, production S0/M2 schema-only upgrade with recorded candidate migrations through `00180`; local Next dev server on `localhost:3017` bound to that branch. Only fictional users and data were used. Production was not written.

## Before and repair

The platform `admin` role's service-role query allowlist included `relief_claims`; `/api/admin/query` accepted arbitrary flat claim columns, and the Relief report requested claim ID, membership ID, amount, event type, status and timestamp. `/api/admin/export` sent one row per claim. A configured service-role key would bypass the R-011 row policy and expose these rows to staff without separate operational claim-detail permission. The isolated local server's service-role key is a placeholder, so this was a demonstrable code/authorization path, not a before-browser disclosure claim.

`00179` adds plan-level counts and amounts with separate pre-aggregation for enrollments, claims and payouts. It is an invoker RPC executable only by `service_role`. `00180` adds a fixed-window, current-staff-scoped wrapper for active platform `admin`/`super_admin`; it checks `auth.uid()` from the current JWT, has no free-form cutoff, and returns no claim/claimant ID, event type, narrative or claim timestamp. The generic service-role query now rejects `relief_claims` for all roles. Relief reporting, usage/engagement counts and aggregate CSV consume the scoped wrapper; the export returns only after its platform audit insert succeeds. Operational claim readers continue to use their current RLS policies. The initial `00179` payout source was wrong; `00181` corrects it below.

## After evidence

- Hosted effective EXECUTE on the inner RPC: `anon=false`, `authenticated=false`, `service_role=true`. An `authenticated` direct invocation failed with `insufficient_privilege` inside a rollback probe. Three existing fictional plans produced three aggregate rows. The initial independent payout comparison used the same empty legacy source and therefore did **not** establish disbursement correctness.
- Before `00181`, a rollback probe inserted two fictional legacy payouts of 3 and 4 against one claim; the initial aggregate rose by 7. This established only the initial function's legacy-table mechanics, not the authoritative F3 payout contract. The probe rolled back, leaving zero legacy payout rows.
- Hosted role probe with `SET LOCAL ROLE authenticated` and fictional JWT subjects returned three plan rows for an active platform admin. A non-staff actor was denied and an invalid range rejected. Anon has no wrapper EXECUTE. The wrapper requires current `platform_staff.is_active`; membership and topology detail paths remain separate.
- The fictional owner `00000000-0000-4000-8000-00000000a801` was given an isolated-branch `admin` platform-staff fixture (`ee76dcf2-389c-4729-8a69-05ff7cdaca6b`). Browser session JWT issuer was the isolated project. As that actor, `POST /api/admin/query` for claim fields returned `403 FORBIDDEN`; `POST /api/admin/relief-aggregate` returned 200 with three plan rows and only aggregate keys; `POST /api/admin/export` returned 200 with a plan-aggregate CSV header and three data rows. Exactly one `export_relief` platform audit entry was recorded. An unauthenticated aggregate POST returned 401. Deactivating that staff row in a rollback probe denied the wrapper using the same JWT subject.
- Mounted FR and EN platform Relief reports rendered the same three plans and aggregate counts at 390 px. The document width remained 390 px; the wider plan table scrolls inside its own container. The report retains per-currency disbursement and plan charts from aggregate inputs and omits the previous recent individual-claims table. This verifies mounted reporting and mobile containment, not all R-011 roles. The initial report incorrectly displayed `$0.00` disbursed for a hosted paid claim; the corrected FR display is recorded below.
- The existing `scripts/test-relief-plan-scope.sql` was run once on the upgraded isolated branch as a rollback-only hosted integration probe. Its distinct branch-reviewer versus owner-payer case asserts that an authorized payer sees the claim but cannot read decision history or review it, while the designated reviewer can view/detail and decide. The script also checks current scope, transfer, topology, cross-tenant and payout invariants. The connector returned only a final SELECT rather than each assertion notice; a separate read found zero fixture users and groups afterward. This is whole-script hosted success, not a per-step hosted transcript or all-role browser acceptance.
- `npx tsc --noEmit --pretty false` and ESLint on the new RPC callers, report, query guard and RBAC module pass. A broader touched-file lint invocation still reports inherited errors in `admin/analytics` (`react-hooks/purity` on unchanged `Date.now`) and `admin/export` (`prefer-const` on unchanged `filename`), plus existing unused-import warnings. The optimized Next 16.2.1 build passed for this code with 46 generated static pages and the new aggregate route; temporary `next.config.ts` and `tsconfig.json` changes were restored. Existing middleware, edge-static and metadata warnings are advisory.

The dashboard OAuth account used in this turn lacked access to the isolated project. No key was copied or saved. The test server's service-role key remains a placeholder; the authenticated wrapper allowed the approved aggregate workflows to be tested without it. With the fictional `admin` role, `/fr/admin/analytics` received `403 FORBIDDEN` from its other generic query tables before reaching a database key check: this is a separate caller/role contract gap, and its zero totals cannot be accepted. The fixture role was then changed to `super_admin`; that removed the role-level denial, but its generic service-role queries still cannot load without the correct branch key. The Relief aggregate itself succeeds. The browser count/chart for those mixed-source pages is therefore not accepted as a full-page pass. A specific branch-key setup request is pending; do not use a production key. A later attempt to demote the fixture was denied by the existing `cannot_demote_last_super_admin` trigger; the fictional super-admin fixture remains only on the isolated branch until branch cleanup. The guard was not bypassed.

**Remaining R-011:** default aggregate versus detail visibility across all app roles, current scope after membership/topology changes in browser sessions, and broader claimant/reviewer/owner-payer browser matrix. The isolated platform-staff fixture is retained for those tests and must be removed by Daybreak Blue when the branch is no longer needed. Production remains HOLD.

## Posted-payout correction, 2026-09-26

**Exact code:** `8499361e333ecbfe459fa3cf6da67622cd2b4e44`. **Hosted environment:** the same isolated branch, with `00181_platform_relief_posted_payout_aggregate` recorded as version `20260926155906` after `00180`. The migration tool initially recorded its name without the `00181_` filename prefix; Daybreak Blue corrected only that one history row's name after verifying its version and single stored statement. No migration was replayed or deleted.

The hosted paid claim `08220e05-eea0-4573-b43f-b1e6b71fcf99` links to posted F3 event `a4117d99-e667-44ed-b339-58eefd05e391`, source `relief`/`claim_payout`, with a `$1.00` expense posting and a `-$1.00` custody posting. `relief_payouts` has zero rows. Thus `00179` returned zero disbursed despite a real fictional payout. `00181` aggregates the claim-linked posted **owner expense** posting for `claim_payout` or `agency_claim_payout_owner`, using the event's posted time; it excludes the delegated branch custody leg and unverified legacy payout rows. It preserves the wrapper and inner EXECUTE grants.

After `00181`, three plan rows return `$1.00` total disbursed, matching an independent posted-event expense sum with zero per-plan differences. The paid plan has one claim, `$1.00` all-time/in-range payout, and one `2026-09` monthly amount of `1`. A rollback-only insert of two legacy rows totaling `$7.00` left the aggregate at `$1.00`; after rollback the legacy table again had zero rows. The inner RPC remains inaccessible to `authenticated` and executable by `service_role`. The mounted fictional-admin FR report at 390 px now displays `$1.00` for the paid plan and USD total, with the other two plans at `$0.00`.

This correction is focused payout reporting evidence. No delegated payout was present in the retained hosted fixture; its owner-versus-branch exclusion still needs a candidate-bound behavioral check. Historical R-012 rows remain quarantined pending finance-owner reconciliation. The earlier optimized build at `157e9eb` covers unchanged application callers, not this SQL migration; the hosted function and mounted report were retested after `00181`.
