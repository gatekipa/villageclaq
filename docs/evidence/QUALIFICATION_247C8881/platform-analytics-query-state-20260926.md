# Platform analytics query state — 2026-09-26

**Executor:** Daybreak Blue. **Code:** `5b748419286e3e4737635a5f8e0f04a86a9d1b6a`. **Environment:** local Next dev server bound to the isolated Supabase branch `nisipxbuvndobyxqqglf`, fictional platform staff only. Its service-role key remains a placeholder; no production key or data was used.

The platform `admin` analytics page requests a 30-day count from `event_attendances`, but the server's `admin` read allowlist omitted that table. The earlier fictional-admin browser attempt returned `403 FORBIDDEN` for the whole batched query, and the mounted page rendered zero counts without showing that failure. The same role already has an attendance report and attendance export permission. The focused repair adds `event_attendances` to this role's read list and makes batch/per-query failure explicit in analytics: the user count becomes `—` and the feature chart is withheld when a query fails. It does not change database grants or the claim-row denial.

TypeScript and diff check pass. Targeted ESLint passes with the preexisting unchanged `react-hooks/purity` rule violation on `Date.now` disabled for classification; six existing unused-import warnings remain. With the fictional `super_admin` fixture and the known branch-key placeholder, the mounted FR analytics page now shows an `Invalid API key` alert, `—` for total users, and a query-error message instead of false zero chart values. This verifies failure-state rendering. The new `admin` attendance allowlist and successful mixed-source data load still require browser verification after a valid **branch-scoped** service key is installed. The isolated branch's last-super-admin guard prevents simply demoting its only fictional super-admin fixture; it was not bypassed.
