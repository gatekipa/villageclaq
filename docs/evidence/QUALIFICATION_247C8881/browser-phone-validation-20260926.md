# VC-04 phone save boundary — 2026-09-26

Executor: Daybreak Blue. Application code `324e37fee8666606d82358ba6d23e7282c5a4ee6`, local French mobile browser at 390×844 on localhost:3017, bound only to isolated Supabase branch `nisipxbuvndobyxqqglf`. Fictional owner `00000000-0000-4000-8000-00000000a801`; external messaging suppressed. No production write.

Before repair, the profile form displayed `France : 9 chiffres requis (3 saisis)` for `+33123`, yet pressing the personal-information Save persisted `profiles.phone=+33123` (observed `updated_at=2026-09-26 14:01:05 UTC`). The save handler ignored the component's visible validation. The same incomplete value could enter onboarding or proxy-member save paths. Switching country while a partial phone existed also reset the selector to Cameroon.

The repair shares the component's exact country-specific completion rule with profile, phone-collection, member onboarding, group onboarding and proxy-member save boundaries. Optional blank values remain allowed where the workflow permits them; required phone collection and member onboarding reject blank. Member edits reject incomplete proxy phones before any role or standing mutation. Changing country clears the prior number and preserves the selected code.

After repair, the French profile showed both the digit-count hint and a localized save error for `+33123`; hosted SQL showed the prior profile and HQ membership phone values and timestamp unchanged. A complete reserved fictional US number `+12025550100` saved to both profile and membership. Switching from that saved number to France kept `+33`; a later incomplete French value was rejected, leaving both stored values at `+12025550100`. Clearing the optional field and saving restored both to `NULL`. This is a browser/hosted pass for the profile path, with code-level boundary review and TypeScript/targeted lint for the other affected callers; their complete browser journeys remain under VC-04.

`npx tsc --noEmit --incremental false` passed. Targeted ESLint on the changed component/profile/member/onboarding pages with `--quiet` passed. Including dashboard layout in lint produced the existing `react-hooks/set-state-in-effect` error at its unchanged line 582; this repair did not touch that effect. `git diff --check` passed. No VC-01/02 financial checks were rerun because this change does not alter those paths.
