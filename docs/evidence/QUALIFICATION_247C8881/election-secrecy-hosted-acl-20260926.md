# E-008 isolated-branch ballot secrecy surface — 2026-09-26

Executor: Daybreak Blue. Read-only catalog inspection of the upgraded isolated Supabase branch `nisipxbuvndobyxqqglf` at application code `6895bd7737c794151122d85e2e1c52b4724f8856`, migrations through `00177`. No production or fixture data write.

Effective `authenticated` and `anon` SELECT grants are false on `public.election_ballots` and `public.election_person_vote_claims`; RLS is enabled on both. All ordinary application roles share the `authenticated` database role, so changing an application membership role cannot grant direct raw-ballot access. `election_electorate` has an authenticated own-user SELECT policy. `election_vote_receipts` has an authenticated own-membership SELECT policy; its `voted_at` and voter identity are separate from ballot choices and there is no shared receipt/ballot identifier. Although the table ACL lists anon SELECT on receipts, its sole SELECT policy is for authenticated, so anon has no rows. No public/API view references the ballot, receipt, or person-claim tables.

Catalog function search found only `cast_ballot`, `finalize_election`, and `get_published_election_results` reading the private ballot/receipt tables. Effective `finalize_election` EXECUTE is false for `authenticated` and `anon`. `cast_ballot` returns vote success without choice linkage. `get_election_vote_status` reads the private person claim but returns only the current user's participation boolean. `get_published_election_results` returns only candidate/option counts after close plus explicit publication to a currently active eligible user; the earlier hosted/browser slice showed an unpublished one-ballot tally hidden until authorized publication. These checks support E-008's current SQL access boundary. They do not prove every UI timing or lifecycle scenario; cancellation, archive, new-version reschedule, broad topology, and any downstream aggregate inference remain in VC-04/E-007–E-011 acceptance.
