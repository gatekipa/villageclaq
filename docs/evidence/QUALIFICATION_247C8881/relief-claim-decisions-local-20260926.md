# R-005 claim decision checkpoint — local production-compatible catalog

Owner: Daybreak Blue. Branch: `codex/qualification-repair-247c8881`. No production, hosted-branch, merge, or deployment write occurred.

## Contract and prior behavior

The controlling [master PRD R-005](https://github.com/gatekipa/villageclaq/blob/050be86c9df3455c66b27bb5853eb786228b4009/docs/VILLAGECLAQ_MASTER_REBUILD_PRD_V1.md) requires authoritative claim transitions, current actor permission, version checks, and immutable decision history. The previous reviewer hook used direct `relief_claims.update()`. On the pre-`00149` local catalog, an active reviewer changed `review_notes` directly in the R-011 scope probe; the catalog granted authenticated `UPDATE` and `DELETE` through the legacy policies and table ACL. No version or decision-history record was required by that write. This is an identified missing R-005 contract, separate from the existing VC-01/02 financial closures.

## Candidate change and focused checks

- `00148` adds the explicit `withdrawn` claim status in its own migration; `00149` adds `decision_version`, private immutable `relief_claim_decisions`, a current-authority `decide_relief_claim` RPC, and a scoped history-read RPC. Ordinary claim `UPDATE`/`DELETE` and history writes are revoked. New claims must start submitted without a preapproved or paid effect. The existing payout RPC's approved-to-paid status change increments the version and writes its history in the same database transaction; its full financial path still requires R-006 integration qualification.
- The reviewer hook now sends the route group, claim, expected version, request ID, status, amount and notes to that RPC. The claimant view offers withdrawal, sends the same route group and version, displays all database statuses, and supplies the canonical claim dimensions on submission. EN/FR keys for the affected member page were added; 44 static member Relief keys exist in both locales.
- A fresh PostgreSQL 17.11 catalog `villageclaq_upgrade_replay34` used production S0/M2 schema-only dump SHA-256 `3b539557f807efca02b92fde9307228c45a6b398abc60a96e000150b1d1bdd4a`, filtered import `63983281e445e84aa14f0086b1696168144967bcbcc283b0c8afd137e5d1af9e`, and synthetic local platform fixture `016170b2147631a4ca391a140ad6ff4ea96db12297668d6c54a65e4306e9151f`. All **36 repository migrations `001170`–`00149` applied in filename order** with `ON_ERROR_STOP=1`. The schema-only `public` dump omits the `auth.users` trigger; the local-only `production-s0m2-local-auth-postlude.sql` restored that production trigger before fictional users were seeded. First probe attempts without this postlude failed on missing `profiles` FK rows; they were fixture failures, not product passes. No migration history rows were recorded by direct `psql`.
- On replay34, `scripts/test-relief-claim-decisions.sql` passed inside a rolled-back fictional transaction: direct reviewer update/delete and direct history insert denied; preapproved member insert denied and valid member submission accepted; submitted→reviewing→approved recorded exactly two immutable versions; same-request replay returned `recovered`; changed payload and stale version conflicted; cross-tenant and revoked reviewer recovery denied; active claimant withdrawal recorded one version. An injected paid-history insert failure left the claim approved at version 2 with no paid decision; a subsequent payout posted one event, two postings, one authoritative audit link and a version-3 paid decision. Repeating payout added no event or history row. `scripts/test-relief-claim-detail-scope.sql -v expected_suspended_visible=false` and `scripts/test-no-ordinary-truncate.sql` also passed on replay34. The older pre-`00146` claim-detail probe still passes its expected-leak assertion on replay30. TypeScript and targeted quiet ESLint pass for the affected hooks/pages.

## Limits and next closure

This is **local R-005 decision and owner-payout integration behavior**, not a full R-005/R-006 or release PASS. Daybreak Blue still owns a hosted history-accurate upgrade on the already billed isolated branch after the pending additional-reset authorization, effective-role and candidate-bound browser checks, and R-006 request identity, owner/delegated authority and full cash/fund matrix. R-002/R-003 plan ownership, R-004 portability, and R-012 legacy cutover remain open dependencies. VC-01/02 stay closed within their original tested scope. Independent GPT-6 Sol review has not run. Production remains HOLD.
