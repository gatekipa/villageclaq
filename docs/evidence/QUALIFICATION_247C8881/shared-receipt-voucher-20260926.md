# F4-007 / FCG-1 shared receipt voucher checkpoint — 2026-09-26

**Executor:** Daybreak Blue. **Core code:** `1fc1b8c105e4bc9833ce61a855b43556f1879761`; **dues conflict presentation:** `4bfa213ff6ca9d5af79ab30dceacc441059290bd`; **mobile form sizing:** `cda9a947d77f05ee078ab2058c1a137f9bbf07cb`. **Environment:** local PostgreSQL 17 replay51 and authorized isolated Supabase branch `nisipxbuvndobyxqqglf`; no production write. This is an executor checkpoint, not an independent-review verdict or complete FCG-1 acceptance.

Jude approved one shared internal voucher when a receipt has no external provider reference. The voucher identifies the same economic receipt across manual and module paths; a second real receipt gets a new voucher even when amount, date, and description match. Historical receipts without reliable source identity remain in the reviewed R-012 cutover manifest. The external reference remains a separate optional field.

## Before and repair

- The preserved [00167 reproduction](fcg-manual-module-overlap-local-20260926.md) showed a Relief module receipt and manual income could each post for one declared source before the guard. The 00167 guard protected only declared overlaps; a new manual income could omit its source.
- Migration `00178_shared_receipt_voucher.sql` requires `reference_metadata.source_voucher` for a new manual income, binds it to the canonical payload/fingerprint, rejects a reused voucher across manual/manual and manual/module postings in either order and across groups in one organization, and keeps old posted intent replay. It uses the existing financial event boundary and audit trigger. New dues and loan source records use their recoverable request ID; Relief receipt, member payment and paid ticket callers expose a voucher that becomes their source ID. No finance or tenant grant was widened.
- A browser attempt with a valid UUID initially failed because four new UI validators omitted one UUID segment. The validators were corrected before the first browser write. This is a real before/after browser regression, not inferred from a source check.

## Behavior observed

| Check | Result |
|---|---|
| Local replay51 after 00177 → 00178 | Migration applied on production S0/M2-derived schema. This local clone has no Supabase migration history and is not a deployment rehearsal. |
| Rollback-only Relief agency source probe | `scripts/test-shared-receipt-voucher.ps1` generated `.vercel/fcg-shared-voucher-probe.sql`; `psql -X -v ON_ERROR_STOP=1` exited 0. Missing voucher, manual-first/module-second and module-first/manual-second conflicts, same-voucher retry, changed-payload conflict, cross-group same-organization conflict, and distinct-voucher second effect passed. Two deliberate manual effects had one audit link each. The inherited Relief agency fixture also completed. |
| Affected local callers | `scripts/test-dues-security-revision-2.sql`, `scripts/test-events-ticket-occurrence.sql`, and `scripts/test-loan-voucher-identity.sql` each exited 0 on replay51. Dues/ticket checks cover posting, retry and atomic audit; loan checks source=request, retry and changed-payload conflict. |
| Hosted migration history | Isolated branch preflight found 0 pending unlinked manual income intents and 2 already posted legacy unlinked intents. `supabase_apply_migration` applied 00178 successfully; history lists `20260926145606 00178_shared_receipt_voucher`. Effective `authenticated` EXECUTE on the private overlap trigger is false; the supported manual prepare RPC remains callable. |
| Hosted rollback probe | The same Relief agency probe ran as one rollback-only transaction against the isolated branch with collision-free fictional IDs and fixture slugs. The SQL call succeeded; a following read found 0 fixture users, confirming rollback. The tool exposes the first SELECT only, so the local notice-level transcript is the stronger per-case evidence. |
| French browser, original and fresh tab | Local `localhost:3017` used `.env.development.local` with `NEXT_PUBLIC_SUPABASE_URL=https://nisipxbuvndobyxqqglf.supabase.co`. Fictional HQ owner posted an $8 manual receipt with voucher `7d79933c-e371-4d07-9540-ff434631af79`, request `de0147f2-33a4-417a-a140-0871d01c3271`, event `26a8d042-bdd3-42eb-bfef-3b7d73f63817`. A fresh tab showed that voucher in recovery and returned `IDEMPOTENT_RETURN_EXISTING` for the same event. Hosted SQL still counted 1 event, 2 postings, 1 private audit link. |
| Deliberate identical detail second receipt | A new browser request `c3d51469-613d-4d06-a96a-2585ae465830` with the same $8, date/time, description, account and category but voucher `06ed344a-b74b-4dbc-b86c-67be5479dd3c` posted event `bbafda1c-c188-4545-89ad-5d37447f36d3`. Hosted SQL found 2 postings and 1 audit link for each distinct receipt. |
| EN dues cross-path denial and separate source | The fictional HQ owner submitted a $5 non-refundable dues payment with the already-posted manual voucher `7d79933c-e371-4d07-9540-ff434631af79`. The duplicate-by-details prompt was explicitly bypassed; `post_dues_record_intent` returned `OCCURRENCE_INTEGRITY`. Hosted SQL found no dues payment for that voucher and no second manual event. The prepared dues intent remained unposted. Recovery labels it “Prepared; not posted”; retry shows the specific conflict. A separate voucher `468965df-b9c4-4b3d-bf75-2a00c7ee889b` posted payment/event `82a9de90-322c-4c4a-84e2-4e0c4598e622` with 2 postings and 1 audit link. Recovery of this posted attempt displayed “no duplicate receipt was created.” The conflict explanation change is code `4bfa213`; TypeScript, touched-file ESLint, message JSON parse and diff check exited 0. |
| Code gates | `npx tsc --noEmit --incremental false`, targeted ESLint `--quiet` for all touched TS/TSX, and `git diff --check` exited 0 after the UUID repair. |
| Optimized build | `npm run build` exited 0 for code `1fc1b8c` using an ignored `.vercel/qualification-build` output directory and authorized Google Fonts network access. This avoided an empty OneDrive reparse directory under the ordinary `.next` cache that caused `EPERM` on cleanup. The temporary `next.config.ts` edit was restored byte for byte and Next's generated `tsconfig.json` edit was removed. Existing middleware, edge-static and `metadataBase` warnings remained advisory. |
| Mobile controls | At 390×844, the EN dues voucher input/generator fit the single-column form. The FR finance dialog had a 356 px popup containing an 887 px form, clipping the title and later fields ([before](fcg-mobile-finance-before.png)). Constraining the popup grid track and form min-width produced a 307 px form and controls within the dialog ([after](fcg-mobile-finance-after.png)); touched-file ESLint, TypeScript and diff check passed on `cda9a94`. This is a mobile layout check, not a mobile payment-posting claim. |

**Limits and next acceptance:** The posting browser checks are FR and EN desktop on a local candidate bound to the isolated branch; mobile voucher controls were inspected at 390×844 without a mobile posting. They do not establish every EN/FR mobile or all-module path. The rollback probe verifies the Relief cross-path trigger against a real compatible catalog, but the hosted SQL connector returned only its first result and did not expose individual notices. A rejected dues preparation persists as an immutable unposted attempt for review; it is not a payment or financial effect. Finish affected module browser paths, revoked/cross-tenant actor probes on the latest schema, Security Revision 2 correction/refund and reporting reconciliation, R-012 historical source decisions, final build for the final code state, then actual GPT-6 Sol independent review. Preserve the billed branch and fictional evidence until qualification no longer needs them.
