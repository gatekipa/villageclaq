# VC-03 / VC-04 scope and upgrade reconciliation — 2026-09-26

**Owner:** Daybreak Blue. **Candidate application code:** `cb5581dff8bb7b9a9b990ede5509bff7d246f21b`; documentation/evidence starting tip `d78d4cf0f031e18398d3fc2d4454d38fb986b118`. This is one continuation of the existing qualification, not a new audit round. VC-01/VC-02 remain closed in their tested scope; their suites were not rerun.

## Authority and milestone mapping

- PR #71 frozen master PRD: `050be86c9df3455c66b27bb5853eb786228b4009`, `docs/VILLAGECLAQ_MASTER_REBUILD_PRD_V1.md`. Security Revision 2 linkage: `a04cdc640234ef7db95e6da3bde8e7f4668062c6`, `docs/VILLAGECLAQ_MASTER_REBUILD_PRD_V1_SECURITY_REVISION_2.md`; it changes F4-002/FCG-1 cash classification, not milestone numbering. Approved growth amendment: `88a1a56c4737dc5e1bf386bcd2ede4bbcd58c7da`, master PRD §31; it adds M13–M15 after M12 without renumbering M0–M12. No approved remapping of M4/M5/M9 was found in these refs.
- Canonical **M4 H-001–H-010 (Hierarchy 2.0)** maps only in part to the current `groups.organization_id/group_level`, `group-switcher.tsx`, and `group-context.tsx`; the local tracker’s “M4 dues” is actually canonical **M5 F4-001–F4-007** code in `00124_m4_01_dues_f3_bridge.sql` and `use-dues-posting.ts`. The production read-only catalog and isolated branch both lack `organization_units`, `unit_closure`, `organization_grants`, and an organization-person identity table. Repository SQL/source search found no implementation of those named H-001 structures. `group-context.tsx` falls back to localStorage when the optional `?group=` parameter is absent and `switchGroup` changes local state without a route scope; that does not establish H-007 route-authoritative tenant context or H-008/H-010 two-tab/stale-draft guarantees. H-006 independent memberships have a narrower implementation.
- Canonical **M5 F4 dues** has an actual ledger bridge, previously exercised on the isolated affected-schema fixture for one confirmation/retry. Security Revision 2 F4-002 and the F4-003–F4-007 matrix still need production-compatible scenario evidence for non-refundable unapplied cash, refundable/conditional credit, later allocation, source overlap, and cross-device dues replay. Do not infer this coverage from the tracker’s “M5 RBAC” label; that code is `00125_m5_01_membership_rbac_canonical.sql`.
- Canonical **M9 E-001–E-011 (Elections 2.0)** has only a narrower legacy/group-election path in `00073_elections_anonymity_fix.sql` and `00126`/`00127` governance SQL. `00126` `cast_ballot` reads a current group membership and standing, then writes a membership-keyed receipt and anonymous ballot. It does not read a frozen person-deduplicated electorate or topology snapshot; repository search found no such tables/columns. The tracker’s “M9 events” is `00130_m9_01_events_ticketing.sql`, a different feature. E-003–E-005 and E-011 acceptance remains open; E-001/E-002/E-006–E-010 need relevant end-to-end evidence, not a label-based verdict.
- Canonical growth §31 M13/M14/M15 maps to files `00134`/`00135`/`00136` and their UI; the amendment's planned status at its historical SHA is not a current code verdict. The isolated fixture has none of their tables, so their approved journeys have no runtime closure here.

## Deployed baseline and migration facts

- Read-only production project `llbnliixczcqfftxpsmb`: `supabase_migrations.schema_migrations` has 32 rows, ending `20260912174049 m2_notification_policy_foundation`, equivalent to repository S0 cut 1–3 plus `00117` M2. Production contains `election_ballots`, `group_audit_logs`, `payments.relief_plan_id`; it lacks F3 `financial_events`, `financial_core.manual_financial_intents`, and `payments.financial_event_id/financial_account_id`. It also lacks H-001 `organization_units`/`unit_closure`/`organization_grants`. No production writes were made.
- The smallest intended release upgrade is the **production S0/M2 schema baseline → candidate forward migrations `00118`–`00137`**, with any demonstrated compatibility repairs reviewed and applied in order. The repository's F3 approved `stub-live-pin`/F24 fixture qualifies `00118`–`00123` runner mechanics only. It expressly disallows a greenfield `00001`–`00116` replay and is not production-equivalent. The lost six-edit fragment remains preserved; its unknown missing lines do not prove a current functional defect.
- The paid isolated branch `nisipxbuvndobyxqqglf` (`2799efdd-2a84-46b6-9cc4-ec8709e0ce39`) was approved for one branch at $0.01344/hour. Its automatic migration failed and left no app schema. A partial historical fixture plus exact `00118`–`00125` and `00137` was installed for VC-01/02. Replay stopped at duplicate index `00032` and absent `election_ballots` at `00126`; fixture-only `00026`/`00030`/`00122` adaptations and read grants are listed in `representative-hosted-00137.result.md`. These failures prove that *this fixture* is not the intended release baseline; they do not establish that an S0/M2 incremental upgrade fails.
- Independently, `00134`/`00136` source has candidate-to-baseline contract mismatches needing correction and execution on a production-compatible floor. `00132` defines outbox columns `group_id`, `membership_id`, `channel`, `template`, `data`, `idempotency_key`; `00134` writes nonexistent `payload`. `00136` calls `has_group_permission(uuid,uuid,text)` while the catalog signature is `(gid uuid, perm_key text, uid uuid)`; it reads nonexistent `memberships.status`, joins nonexistent `group_permissions`, and inserts nonexistent `recipient_id`, `template_id`, `payload` in the outbox. **Severity: P1 release correctness blocker for the approved M13/M15 journeys**, based on exact source/catalog mismatch; runtime confirmation awaits the intended baseline. This is not a claim of a successfully executed `00134`/`00136` migration or a complete list of later defects. Treat M13/M15 activation/request acceptance as HOLD pending focused repair and real-baseline execution; do not widen grants as a workaround.

## Candidate-bound browser continuation

- Local Next dev server `http://localhost:3100` used only `.env.development.local` from the earlier verified branch-bound backup; `.env.local` absent. The authenticated fictional officer's `/en/dashboard/members` route displayed two fictional members, search for “Fictional Member” reduced results to one, and `?addProxy=true` opened the authorized proxy form then removed the query parameter. The Cameroon phone selector reported “9 digits required (8 entered)” and cleared that warning for 9 digits. The form was closed without creating a record.
- A fresh login as the fictional ordinary member hid admin navigation; direct `/en/dashboard/roles` displayed “Access Denied”; `/en/dashboard/members?addProxy=true` did not open the restricted proxy form. `/fr/dashboard/members?addProxy=true` rendered the French member route and likewise did not open the form. These are browser authorization/navigation checks, not H-010 hierarchy closure.
- For the ordinary group-one member, direct `/en/dashboard?group=00000000-0000-4000-8000-000000000002` kept the selected group-one label and showed no group-two data. The URL nevertheless retained the unauthorized group-two parameter, so the UI does not give an unambiguous route-authoritative action scope; this is limited H-007/H-009 evidence, not a full two-tab or server authorization proof.
- The French route repeatedly showed a recoverable hydration error in Next dev: server `Loading...`, client `Chargement...` at `DashboardGuard` in `src/app/[locale]/(dashboard)/layout.tsx:468`. One narrow `setRequestLocale` experiment did not clear it and was fully reverted; candidate code was unchanged. The page recovered and displayed French content. Classify this as a VC-04 EN/FR browser defect for investigation (P2, no demonstrated data loss), not a passed locale acceptance. The partial fixture also logged missing read grants/functions and `payments.relief_plan_id`; those are fixture limitations, not evidence of a VC-01/02 regression. An invalid-code callback navigation while signed in ended at the same-origin dashboard; it does not qualify a real auth-code callback. Two-tab hierarchy, real callback, broader viewport matrix, and M13–M15 journeys remain open.

## Bounded next execution and ownership

Daybreak Blue should preserve this branch and its evidence, obtain a schema-only S0/M2 production baseline through founder-approved read-only export, then use a separately authorized reset of this branch (or an approved new branch) to load that baseline and apply `00118`–`00137` in order after targeted `00134`/`00136` repairs. No production data, production write, branch merge, or main deployment is in scope. Reset would destroy this branch's fictional rows and partial schema, so it needs Jude's specific approval after evidence is preserved. The existing billed branch remains available for focused checks; Daybreak Blue owns cleanup once the production-compatible isolated qualification and independent review no longer need it, with Jude confirming the timing/cost decision. No reset or deletion was performed here.
