# M13 introduced-group activation — 2026-09-26

Executor: Daybreak Blue. Tested application code: `6966c818cb64d8873f716384eb586ab2da949d8d`; mounted FR browser at `http://localhost:3017`, 390×844, bound to the existing isolated branch `nisipxbuvndobyxqqglf`. Fictional actors only; external messaging remained suppressed by the invalid local service-role placeholder. The introduced group and referral were created by the previously recorded mounted existing-account onboarding journey, not by this probe.

The referral `44ca7f3d-3125-417f-ade7-209778ea16ff` for fictional group `ff70ac2a-e3b8-4daa-b720-434236f82a68` began `claimed`, with `activated_at=null` and one owner membership. A second active fictional membership (`11111111-1111-4111-8111-11111111c913`) was seeded in the isolated branch; referral status remained `claimed`. This demonstrates that a new member alone did not qualify.

The owner then created meeting `2bfd5f4c-829b-44b0-8694-2c110960a81d` through the mounted FR events form. Because that form correctly rejected a past start date, the probe staged this **fictional event's** start/end in the past and its status as `completed` directly in the isolated database. The referral still remained `claimed`. This direct fixture adaptation did not test the event-completion UI or its authorization.

The mounted FR attendance form listed the two group members and the completed meeting. The owner marked only the non-owner member present and saved. The page showed one present, one absent, 50%, and two attendance records. Hosted reads then showed one qualifying `present` record and the referral `activated` with `activated_at=2026-09-26 16:42:01Z`; there was exactly one activated claim and **zero** new HQ memberships for the invited actor. At 390 px, the attendance page document width was 390 px and the meeting history remained visible. A current authorized platform operator call to `get_referral_activation_report` returned at least one unique activated organization with no person/email field; the read-only assertion returned `M13_ACTIVATED_REPORT_PASS`.

The existing rollback suite `scripts/test-growth-referral-activation.sql` separately covers altered/replayed tokens, revoked/expired claim denial, core activation, operator report permission and revocation of activated history. This checkpoint closes the mounted FR attendance-to-activation integration for one fictional group. Remaining M13 acceptance includes event-completion UI, two-tab behavior, EN/FR keyboard and other mobile routes, and successful mixed-source admin report rendering once a valid **branch-scoped** service key is available. No production operation or external send occurred.
