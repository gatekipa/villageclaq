# F4-007 / FCG-1 declared-source overlap — local checkpoint

Executor: Daybreak Blue. Repair commit `bd9e25a4c1cd6085c199adb6013e89d856600c2d` on `codex/qualification-repair-247c8881`; the mounted R-010 error state is in `ba7d6ee30e0be5ac73b009c3260100f5905f9154`. No independent review has run. Production was read only; no hosted branch was changed.

**Before:** `scripts/reproduce-fcg-overlap.ps1` injected a fictional owner Relief agency receipt and a manual money-in command with the same payment UUID in `reference_metadata.reference` into the rollback-only agency fixture. On local replay42 before `00167`, the manual command returned `POSTED` and the owner had **two income postings of 70**: one `relief` owner recognition and one `manual_finance`. Ignored log: `.vercel/fcg-overlap-repro.log`. No amount/date heuristic was used.

**Repair:** `00167` adds a private `financial_events` insert guard. It serializes on group plus declared source, rejects a manual reference already used by a module event, and rejects a module event if a manual event already declares its source. A migration preflight refuses previously overlapping declared links. The trigger function is not directly executable by `anon` or `authenticated`; authenticated direct event INSERT remains false. Existing immutable local ledgers and module effect classifications are unchanged.

**After:** Fresh local PostgreSQL 17.11 UTF-8 replay43 at `localhost:55432` imported the preserved production S0/M2 schema-only baseline SHA-256 `3b539557f807efca02b92fde9307228c45a6b398abc60a96e000150b1d1bdd4a`, filtered import `63983281e445e84aa14f0086b1696168144967bcbcc283b0c8afd137e5d1af9e`, synthetic platform fixture `016170b2147631a4ca391a140ad6ff4ea96db12297668d6c54a65e4306e9151f` and local auth postlude. All **54** migrations `001170`–`00167` applied in filename order with `ON_ERROR_STOP=1`; ignored list `.vercel/local-replay43-apply.names`. The default reproduction and `-ManualFirst` each failed with `OCCURRENCE_INTEGRITY` in `.vercel/fcg-module-first-replay43.log` and `.vercel/fcg-manual-first-replay43.log`. A deliberately separate manual reference of identical amount posted, its repeat request returned the existing result, and it had exactly one authoritative audit link (`.vercel/fcg-separate-replay43.log`). The rollback-only `scripts/test-relief-agency-receipts.sql` and `scripts/test-dues-security-revision-2.sql` both passed on replay43, including dues classification, retry, and audit-fault rollback. One parallel attempt deadlocked because both scripts used DDL in the same local catalog; the sequential rerun passed. This was test-harness interference, not a product failure.

**Limit:** The guard protects **declared equal source references**. Manual source references remain optional and module source mapping is not yet complete across all adapters; a newly invented manual reference cannot be proven to represent an already recorded real-world receipt. F4-007/FCG-1 therefore remains open pending required source declaration/binding, cross-path tests, historical reconciliation, and hosted effective permissions. Direct SQL replay did not write Supabase migration history and is not a hosted deployment rehearsal. VC-01/02 remain closed in their previously tested scope; the newly affected manual insert path received a focused retry/audit check here.
