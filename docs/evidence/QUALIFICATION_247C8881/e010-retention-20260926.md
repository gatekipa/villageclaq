# E-010 election evidence retention

Executor: Daybreak Blue. Isolated Supabase branch `nisipxbuvndobyxqqglf`, upgraded production S0/M2 schema-only fixture through candidate migration `00184`. No production records or writes. The fictional `scripts/test-elections-2-0.sql` transaction rolls back all rows.

The existing E-001–E-011 rollback suite was extended only at its retention boundary. It cast one ballot each in local, federation and officer elections, cancelled the federation election, closed the others, and published the closed results through their separate authorized path. After those lifecycle actions, the fixture still had three ballots and three vote receipts. A direct delete of the cancelled election raised `ELECTION_HISTORY_IMMUTABLE`. The probe then applied the same persisted group fields as the platform admin archive action (`status='archived'`, `is_active=false`, `archived_at`, `archived_reason`) and found all three elections, ballots and receipts intact. The hosted result was `E010_RETENTION_PASS`; the transaction rolled back.

This proves retention for the tested close, cancel and group-archive states on the real upgraded catalog. It does not test the mounted admin archive request or long-term retention jobs. The local branch service-role key remains a placeholder, so that browser path is still VC-04 work. This is executor evidence, not independent Sol review.
