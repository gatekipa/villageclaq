# Representative hosted qualification for migration 00137

**Owner:** Daybreak Blue. **Date:** 2026-09-26. **Candidate code:** `cb5581dff8bb7b9a9b990ede5509bff7d246f21b` on `codex/qualification-repair-247c8881`; core repair remains `4f90266e9716ad11c55fce6443359a1260f9e172`. **Verdict:** PASS for the named financial and audit paths on the isolated fixture; broader release qualification remains HOLD.

## Environment and boundary

- Jude approved one billed Supabase development branch under VillageClaq project `llbnliixczcqfftxpsmb`: branch `2799efdd-2a84-46b6-9cc4-ec8709e0ce39`, project ref `nisipxbuvndobyxqqglf`, quoted at $0.01344/hour. This branch copied schema without production data. No production database write, main merge, or production deployment occurred.
- The new branch's automatic migration step reported `MIGRATIONS_FAILED` and left no application schema. The smallest relevant setup applied real repository `00001`–`00025`, `00027`–`00029`, `00031`, `00035`, and `00118`–`00125`, then the exact candidate `00137`. Historical `00026` needed an idempotent policy drop before its duplicate `rls_pa_select` create; `00030` needed the scalar `get_user_group_ids()` call in place of `unnest(uuid)`; `00122` needed a branch-only `public.uuid_generate_v5` wrapper for the extension function. Clean replay stopped at the duplicate index in `00032`, and the later sequence stopped at `00126`'s absent `election_ballots`. These branch-only compatibility steps and the incomplete historical chain mean this is a representative affected-schema fixture, not a clean `00001`–`00137` replay or production-equivalent M15 environment.
- The relevant F3 permission helper was pinned to the production function definition already used by the prior F3 qualification; the two MD5 values matched its preflight. Branch-only membership/profile/group SELECT grants and policies were needed because the stopped historical sequence had left self-recursing membership access and omitted effective read grants. No financial or tenant permission was widened for the candidate repair.
- Three fictional auth actors (group-one officer, group-two officer, group-one ordinary member), two fictional groups, finance roles, account, fund, category, and a dues payment were seeded. External messaging was suppressed. The local Next development server at `http://localhost:3100` loaded only `.env.development.local` with the branch public URL/key and an invalid service-role placeholder. `.env.local` was absent during browser execution. A compiled client chunk contained branch ref `nisipxbuvndobyxqqglf` and no production ref. The existing Vercel Preview environment inherits production Supabase variables and was not used for writes.

## Database behavior on real affected schema

1. Request `00000000-0000-4000-8000-000000000601`: prepare, post, then list/replay from another session context returned `POSTED` then `IDEMPOTENT_RETURN_EXISTING` with the same event `0757bcbb-93d6-4c52-98ad-018f305024f4`; one event, two postings, one trusted audit/link.
2. Request `...0602` with identical financial details created a distinct event `fd549f08-114b-4a59-9db1-54818883dd77`. Changing amount under `...0601` raised `CONFLICT`. Foreign tenant recovery was denied.
3. Revoking the fictional officer's `finances.manage` denied both list and replay, leaving the intent pending. After restoring the permission, retry was possible. A direct ordinary-member audit INSERT and authenticated direct `post_financial_command` EXECUTE were denied with `42501`.
4. A test-only trigger raised `INJECTED_AUDIT_FAILURE` on `financial_event.posted`. The attempted post left event/posting/trusted-audit counts unchanged and the intent pending; after removing the fault, retry posted once with one audit. Concurrent retries of request `...0604` produced one `POSTED` and one `IDEMPOTENT_RETURN_EXISTING`, sharing event `df73844d-867e-4175-b545-271a18b0c8fd`.
5. The affected M4 dues caller `post_dues_payment_confirmation` posted a fictional $75 payment once, created one F3 event and authoritative audit, and returned existing on retry. An ordinary client report could supply `reported_action=financial_event.posted` but persisted only as `action=client_activity`, `entity_type=client_report`, with an unverified description. The activity consumers do not promote that claimed action to trusted posting evidence.

## Candidate-bound browser behavior

- Authenticated fictional officer in Chrome against the local branch-bound candidate. On a fresh browser login, Record Transaction listed server-stored posted intents. Recover/retry returned `IDEMPOTENT_RETURN_EXISTING` with the original event; no new posting/audit was created.
- The officer used Record Another twice for the exact same $500 action, account, category, time, and description. The two new request IDs `481d1144-3fb8-441b-94d7-c5ad67a40dae` and `4da40b97-d2a6-4288-828b-e91459964ecb` posted separate events `c4fd1936-9655-41ad-8d28-2cc41e75dc1f` and `b1e4f345-2eca-401e-a4d7-748044fc4163`; each has its own audit link.
- A prepared $125 request `...0605` appeared as “Pas encore enregistrée” in the 390×844 French mobile recovery dialog. Revoking `finances.manage` made its retry show the French permission error, with no posting. After permission restoration, the same intent posted event `97ab4d3a-d815-4ede-9389-0b0d7e8e74dc`. The permission fixture was restored.
- The real audit table's `actor_id` has a foreign key to `profiles`, while both client activity consumers had joined `memberships`; the Activity Log therefore showed an incorrect empty state before the focused caller repair. After changing both consumers to the actual profile relationship, the English Activity Log displayed all eight records, the Financial filter displayed only the seven then-existing authoritative records, and `client_activity` showed an explicit “Unverified report” badge. The French mobile Activity Log and dashboard showed translated posting labels and “Déclaration non vérifiée” for the client report. Query errors now surface as errors rather than false empty states.
- Final isolated counts after the mobile pending recovery: **8 events, 16 postings, 8 `financial_event.posted` audits, 8 private audit links, 1 distinct client report**. The fictional officer's finance permission was restored. The browser viewport override was reset.

## Limits and follow-up

The browser exercised original submission, fresh-session replay, a deliberate second identical transaction, revoked-authority failure, pending recovery, audit visibility, and relevant EN/FR mobile states. Database calls exercised cross-tenant denial, changed-payload conflict, concurrent retries, direct forgery denial, audit rollback, and the M4 dues bridge. This branch does not establish clean migration replay or the remaining M15 browser journeys (admin, navigation, search parameters, callbacks, phone input, full tenant boundary, all mobile layouts). The dashboard showed other failures from the deliberately partial historical schema, including a missing `payments.relief_plan_id` column and omitted read grants; those are environment limitations for VC-04, not evidence that `00137` fails. F24 remains applicable only to its documented `00118`–`00123` fixture mechanics.
