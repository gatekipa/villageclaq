# R-006 / R-010 delegated payout and matched management projection

Tested code: `f95eaba4e5cfa936c5a5bb602f4531a37c531029` on `codex/qualification-repair-247c8881`.

Fresh local PostgreSQL 17.11 UTF-8 database `villageclaq_upgrade_replay45` at `localhost:55432` imported the preserved production S0/M2 schema-only baseline (SHA-256 `3b539557f807efca02b92fde9307228c45a6b398abc60a96e000150b1d1bdd4a`), filtered import (`63983281e445e84aa14f0086b1696168144967bcbcc283b0c8afd137e5d1af9e`), synthetic platform fixture (`016170b2147631a4ca391a140ad6ff4ea96db12297668d6c54a65e4306e9151f`), and local auth trigger postlude. All **59** repository SQL files from `001170` through `00172` applied in filename order with `ON_ERROR_STOP=1`; ignored names/logs are `.vercel/local-replay45-apply.names` and `.vercel/local-replay45-*.log`. Direct SQL did **not** write Supabase migration history. This is a local compatibility and behavior check, not a hosted deployment rehearsal.

The rollback-only fictional `scripts/test-relief-agency-receipts.sql` passed on replay45. A designated branch payer posted one approved claim under the current frozen scope: branch liability +15/custody -15 and owner expense +15/receivable -15, with two events, four postings, two authoritative server audit links, and one paid claim. A same-command retry returned the existing events with zero new postings. Changed branch fund conflicted; an unrelated owner restricted fund failed and rolled back. The branch's recognized agency balance reserved pending remittances and the payout, rejecting an over-remittance. An injected owner-audit failure rolled back both events, link and paid state; retry after removing the test fault posted once. Revoked, stale-topology and cross-tenant payout replay and owner-fund catalog discovery were denied. The owner payout path still passed `test-relief-plan-scope.sql`; claim decisions, detail privacy, receipt boundary and Security Revision 2 dues probes passed sequentially on replay45. Effective local grants: public payout RPC executable by `authenticated`; private delegated helper and link table unavailable to `authenticated`; owner-fund catalog unavailable to `anon` and current-authority checked for `authenticated`.

On replay44 with the same production schema baseline and migrations through `00172` (the last migration added incrementally), a committed fictional fixture plus a three-second test-only event trigger forced two simultaneous fresh connections to post the **same** claim. Both calls exited 0: one `POSTED`, one `IDEMPOTENT_RETURN_EXISTING`; the database retained one payout link, two events, four postings and two audit links. The delay trigger was removed. Ignored setup, connection and count logs are `.vercel/local-replay44-concurrency-*.log` and `.vercel/local-replay44-concurrent-payout-*.log`.

**R-010 correction to the earlier replay42 checkpoint:** its paired 20/20 example was true only before another branch remittance was posted. The original `00166` projection wrongly ignored a **posted branch remittance pending owner confirmation** and could eliminate an unrelated residual. `00170` now counts posted branch and owner sides separately and eliminates only the still matched reciprocal balance. The replay45 probe shows pending branch remittance: branch due 0, owner receivable 20, elimination 0; an unrelated branch receipt then leaves 10/20 with elimination 0; a further recognized pair yields 30/40 with elimination 20; delegated payout 15 yields 15/25 with elimination 5 and preserves the 10/20 unrelated residual. Local events are unchanged by projection reads. Branch and cross-tenant actors are denied; stale topology returns no rows; prior currency/as-of negatives remain in the probe. The prior evidence remains historical evidence for its specific scenario, **not** proof of the later pending-remittance case.

The mounted claims page now fetches current payout scope, requires branch and owner restricted funds for delegated payout, and blocks stale scope; a narrow current-authority RPC exposes only eligible owner fund names/IDs to the designated payer. EN/FR labels were added. `npx tsc --noEmit --pretty false` and targeted ESLint passed. `npm run build` passed with a temporary alternate output directory and approved network access for the four declared Google Fonts; that temporary config and generated output were removed. The first normal build attempt hit an existing OneDrive `.next/diagnostics` permission error; the sandboxed alternate build could not reach Google Fonts. Neither was an application compile failure.

**Open:** history-accurate hosted upgrade/effective permissions, candidate-bound browser EN/FR/mobile payout and management display, wider R-009 cash classes/R-012 legacy cutover, and independent review. Existing isolated Supabase branch `nisipxbuvndobyxqqglf` was not reset or written in this checkpoint; specific further-reset authorization remains pending. Production was read-only. VC-01/02 remain closed only in their prior tested scope; these focused Relief/dues probes rechecked affected F3/audit integration.
