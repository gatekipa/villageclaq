# F4-001 operational dues waiver

Executor: Daybreak Blue. Authorized isolated Supabase branch `nisipxbuvndobyxqqglf`, production S0/M2 schema-only baseline upgraded through `00187`. Fictional actors only; external messaging suppressed. Production remained read only. This is executor evidence, not independent Sol review.

Before the repair, the dues views recognized a `waived` obligation, but no mounted dues workflow created one. Effective `contribution_obligations` UPDATE and INSERT policies allowed active group admins and `finances.record`/`contributions.manage` actors to write rows directly; those writes had no required authoritative waiver audit.

`00185` adds `waive_dues_obligation(uuid,text)`: it locks the obligation, checks current `contributions.manage` for the group, requires a reason, refuses existing or pending payment links and nonzero paid amount, then updates status/actor/time and inserts a server-authored `dues.obligation_waived` group audit record in the same transaction. A retry returns `ALREADY_WAIVED` after rechecking authorization. It posts no cash or journal event. An invoker-context trigger denies direct waiver updates by ordinary roles. `00186` covers direct INSERT of an already-waived row. The existing group-audit SELECT policy permitted a suspended admin to read waiver reasons; `00187` requires current active owner/admin membership. Hosted migration history records `00185_dues_obligation_waiver_command` / `20260926190809`, `00186_dues_obligation_waiver_insert_guard` / `20260926191120`, and `00187_audit_read_requires_active_admin` / `20260926191710`. The RPC is owned by `postgres`, SECURITY DEFINER, executable by `authenticated` but not `anon`; the two waiver triggers are present. Existing finance/table grants were not widened.

The extended `scripts/test-dues-security-revision-2.sql` passed as an isolated hosted rollback. Assertions reject direct waiver UPDATE and INSERT (`42501`), a paid obligation, a cross-tenant actor, and replay after actor suspension. One authorized waiver and its retry produced one audit and zero financial events. An injected audit INSERT failure rolled back the waiver; retry after removing the fault produced one status change and one audit. An active admin read both waiver audits; the same actor, suspended, read zero. All fictional test rows rolled back.

For the mounted French journey, the existing fictional HQ receipt fixture covered two earlier dues obligations, so an additional unpaid $5 due on `2026-10-03` (`11111111-1111-4111-8111-11111111a917`) was seeded in the isolated branch. At 390 px, `/fr/dashboard/contributions/unpaid` showed $5 outstanding, an explicit waiver action and reason dialog. Confirmation removed it from outstanding without horizontal document overflow. Hosted verification found the obligation `waived`, amount paid `0`, waiver actor/time present, exactly one `dues.obligation_waived` audit record and zero financial events. The fictional fixture remains on the billed branch for qualification; it is not a production record. The mounted English action and export views were not exercised here.

`npx tsc --noEmit --incremental false`, targeted `eslint --quiet`, `git diff --check`, and `npm run build` exited 0 for the waiver application code. The build used a fresh ignored output directory for OneDrive cache compatibility, and the temporary Next config/generated TypeScript config changes were restored. Existing middleware, edge-static and metadata warnings remained advisory. Wider F4 correction and role/browser acceptance remains VC-04 work.
