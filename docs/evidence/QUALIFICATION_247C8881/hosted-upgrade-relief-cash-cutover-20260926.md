# Hosted upgrade and Relief cash checkpoint — 2026-09-26

Executor: Daybreak Blue. Code under test: `680f6a091b1a908cab8586bbe19df5a6e4a6c9d9`. Isolated Supabase branch: `nisipxbuvndobyxqqglf` (branch ID `2799efdd-2a84-46b6-9cc4-ec8709e0ce39`); production `llbnliixczcqfftxpsmb` remained read only. Browser: localhost:3017, `.env.development.local` verified against that isolated branch, with fictional Auth actors and external messaging disabled. The production-bound Vercel Preview was not used for writes.

## Upgrade path and limits

After the specifically approved reset, the preserved production S0/M2 **schema only** was imported into the existing branch. Source SHA-256: `3b539557f807efca02b92fde9307228c45a6b398abc60a96e000150b1d1bdd4a`; filtered import SHA-256: `63983281e445e84aa14f0086b1696168144967bcbcc283b0c8afd137e5d1af9e`. The import excluded production rows, credentials and secrets. It omitted 12 `supabase_admin`-owned default-privilege statements that the branch's SQL role could not replay; explicit grants and policies were retained. A synthetic local Auth/platform postlude was needed for the schema-only fixture. These adaptations prevent calling the fixture production equivalent.

The branch's first 32 migration version/name identities were matched to the read-only production migration history, with empty statements marked `qualification_s0m2_snapshot`. The 64 candidate files `001170`–`00177` then applied in filename order through Supabase's migration tool. Branch history now has 96 rows, ending at generated version `20260926125713`, name `00177_relief_legacy_cutover_quarantine`. The management API assigns timestamp versions; names preserve repository file identity. This is an ordered hosted schema/permission rehearsal, not a CLI `db push` or a production-data replay. The older F24 fixture result retains its documented limits.

Hosted effective rights: `authenticated` can execute `public.settle_relief_receipt(jsonb)` but cannot execute private `financial_core.post_module_pair`, insert `group_audit_logs`, or insert/update/delete `financial_core.relief_legacy_cutover_plans`. The three quarantine triggers on receipt, payout and remittance paths are enabled. Managed `storage.objects` and `storage.buckets` still report ordinary-role `TRUNCATE`; Jude's storage-owner/provider resolution is pending and no permission was widened to pass tests.

## Behavior before and after

The pre-repair local replay46 probe failed `UNRESOLVED_REFUND_SPENT_RECOGNIZED_CASH`: refunding an unresolved receipt incorrectly consumed unrelated recognized cash availability. The corrected replay47 rollback-only probe passed `RELIEF_CASH_SETTLEMENT_PASS` and `RELIEF_CASH_AUTH_PASS`, covering conditional liability, owner/branch recognition or refunds, remittance-before-resolution followed by owner refund, audit-fault rollback and retry, conflicting action, cross-tenant and revoked replay. The existing agency/delegated payout probe passed `RELIEF_AGENCY_PASS`, `RELIEF_REMITTANCE_PASS`, `RELIEF_DELEGATED_PAYOUT_PASS`, `RELIEF_DELEGATED_AUDIT_ATOMIC_PASS`, and `RELIEF_DELEGATED_AUTH_PASS`. Both rollback-only SQL probes also passed on the upgraded hosted branch through 00176 before fictional browser users were committed. Their source scripts are `scripts/test-relief-cash-settlement.sql` and `scripts/test-relief-agency-receipts.sql`; local raw logs remain in ignored `.vercel/local-replay47-*` files. They are focused acceptance evidence, not exhaustive Relief 2.0 qualification.

On the candidate-bound browser, a fresh session recovered manual-finance request `a96a991b-d02e-4d72-aa10-f36205e17efa` as the same posted event `1f86d653-f927-47a1-9534-24c4c7933fc4`. Explicit **Record Another** used request `3e9269ae-6934-4ed4-aff6-d0947df33c4b` and posted a distinct event `c07b5a6b-8802-4a90-bd17-83db090adf97` with the same $7.00, date, description and reference. Hosted query showed two postings and one private server-audit link per event, with no third effect from recovery. This rechecks VC-01/02 integration without reopening their earlier closed scope.

The English browser then confirmed a fictional $3.00 conditional owner receipt as custody `+3` and liability `-3`, followed by a separate recognition event that reverses liability and records income; both events have two postings and one audit link. After 00177, the branch captured one existing fictional uncontracted plan into quarantine. A new receipt for it returned `RELIEF_LEGACY_CUTOVER_REQUIRED`; rollback-only claim-payout and remittance probes returned the same guard. Its prior audited receipt remained readable. A newly created scoped plan was not quarantined. On the French 390×844 browser, the new plan enrolled a fictional member, confirmed a $2.00 refundable receipt, then refunded it from owner custody. The two events have four postings and two audit links total; custody and liability each net to zero and no income posting exists. The old plan is labeled pending scope and absent from the new-receipt selector. No horizontal document overflow was observed at 390 px. The affected EN/FR Relief and finance labels now resolve rather than rendering translation keys; the French finance console returned no errors in the final visit.

## Historical source decision and remaining acceptance

Jude approved a bounded read-only production manifest. It found **two inactive legacy plans** under one owner. One has one enrollment, two claims, zero Relief receipt rows, zero payout rows, and four remittances from one collecting group: two `confirmed` at 250,000 XAF each and two `disputed` at 90,000 XAF each. The other has one enrollment and no receipts, claims or remittances. Same amounts do not prove duplicates. The source does not establish whether confirmed remittances reflect settled cash or the opening custody/owner balances. The migration records unresolved pre-existing plans privately and blocks new receipts, payout effects and remittances until a reviewed cutover manifest is authorized; it does **not** classify these production amounts or post an opening entry. Jude/finance-owner source reconciliation remains R-012 acceptance.

## Incremental claim/payout browser probe at `30e212ac38f1c1ee581d422d4bd8bfb8a800ab91`

The French HQ browser approved a fictional $4 claim on the pre-cutover shared plan. Its payout form accepted an account/fund selection, but the server returned `RELIEF_LEGACY_CUTOVER_REQUIRED`; no payout event was created. This is the expected quarantine boundary, and the approved claim remains unresolved pending the finance-owner cutover decision.

For a positive workflow, Daybreak created a separate fictional HQ unit plan through the UI with $10 coverage and **zero** waiting days, then enrolled a fictional active member. This fixture is `Fictional Immediate Payout Plan` (`38927059-3640-444e-a512-c6b34fa65c16`); it did not modify historical plans or permissions. The browser submitted a $1 claim (`08220e05-eea0-4573-b43f-b1e6b71fcf99`), approved it, selected HQ Cash and HQ Relief, and showed `Payé` / `Posted to Ledger` after payout. Hosted row `financial_event_id=a4117d99-e667-44ed-b339-58eefd05e391` has `source_module=relief`, `effect_kind=claim_payout`, two postings and one private authoritative audit link. The postings are restricted-fund custody −1 and expense +1. This verifies the owner payout slice of R-005/R-006 on the upgraded branch; delegated branch payout, recovery after response loss, every-role detail privacy and notification dispatch remain open. The French form still shows some English labels and UUIDs after selection; this is advisory UI debt, not evidence of a wrong financial effect.

`npx tsc --noEmit --incremental false`, targeted changed-TSX `eslint --quiet`, and staged `git diff --check` passed for this code commit. Remaining VC-04 browser/database journeys, full F4/FCG-1 source reconciliation, managed Storage grants, and actual independent GPT-6 Sol review are open. Production remains HOLD; no main merge or production deployment occurred.
