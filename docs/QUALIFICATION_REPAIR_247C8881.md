# VillageClaq qualification repair — consolidated result

**Owner:** Daybreak Blue. **Scope verdict:** HOLD. **Production:** HOLD pending qualification and Jude's explicit release approval. This is a non-production repair from exact handoff `247c8881b875b7f013254cc3efc6ddc16bcfc3c3` on `codex/qualification-repair-247c8881`. Original audit candidate `f5470a5a8659b44a304eb4c7f9550a19bf7ff991` and reported baseline `e192b55709fa9bab92c1f6311312df38653902d9` were verified as ancestors. The final packaged commit is identified by the branch tip and in Daybreak's handoff; no main merge, production migration, or deploy is part of this result.

The controlling [Master Rebuild PRD v1](https://github.com/gatekipa/villageclaq/blob/050be86c9df3455c66b27bb5853eb786228b4009/docs/VILLAGECLAQ_MASTER_REBUILD_PRD_V1.md) and approved revisions govern P-009, F3-07, SEC-005, the applicable G-009 atomicity rule, and the universal high-risk matrix. The M15 runbook supplements these. The review cap in `AGENTS.md` remains in force; no Astra independent round completed.

## Focused repairs

| Finding and contract | Repair | Executed result |
|---|---|---|
| P1: P-009 / F3-07 durable identity | Migration `00137` adds a private, actor- and tenant-bound manual financial intent storing the exact command under its request UUID. The new prepare/list/post RPCs recheck current `finances.manage`, including after an intent-row wait. The dialog loads recoverable intents before offering a distinctly labeled separate transaction; a fresh device can replay the stored command. The F3 ledger's existing identity and fingerprint rules remain authoritative. | Isolated PG17: same intent after a lost response returned the existing event (one event/two postings); explicit second intent with identical amount/date/details posted a second event; changed payload conflicted; concurrent same-intent posts yielded one `POSTED` and one `IDEMPOTENT_RETURN_EXISTING`; foreign tenant/actor and revoked actor were denied, including revocation during the lock wait. |
| P1: SEC-005 / required financial atomic audit | Migration `00137` removes ordinary authenticated direct audit INSERT and direct `post_financial_command` EXECUTE. A private trigger writes one server-authored `financial_event.posted` row and private event-to-audit link on every new F3 event, inside the event's transaction. Manual posting requires that trusted link and no longer makes a separate client audit write. Existing best-effort callers use `record_client_activity`, which fixes actor and stores claims only as `client_activity` / `client_report` with an unverified description. | Isolated PG17: ordinary forged audit INSERT and direct legacy posting were denied; one trusted row existed for each posted event, including a bridge-style event; injected audit INSERT failure rolled back the event and postings; retry after removing the fault succeeded with one success audit; concurrent replay did not add another audit. |

The approved PRD does not require deduplicating by financial details alone. The implemented default is a durable, server-stored intent owned by the original actor, with an explicit new-intent action for a legitimate second identical transaction. No unresolved product choice was used to stop this repair.

## Evidence and limits

- **Before:** [Original lost-response reproduction](evidence/QUALIFICATION_247C8881/fresh-device-finance-retry.sql) and [result](evidence/QUALIFICATION_247C8881/fresh-device-finance-retry.result.txt) show that a fresh request UUID with the same financial details created a second event. Direct authenticated audit INSERT was possible under the effective `00035` grant and `00114` member policy; the prior manual UI wrote audit after posting in a separate best-effort call.
- **After:** [Regression driver](evidence/QUALIFICATION_247C8881/manual-intent-regression.mjs), [audit stub](evidence/QUALIFICATION_247C8881/manual-intent-audit-stub.sql), and [result](evidence/QUALIFICATION_247C8881/manual-intent-regression.result.txt). The final run exited 0 on an isolated loopback PG17 database with the relevant F3 floor, audit-table stub, and `00137` schema. It is behavioral evidence for these paths, not a full 00001–00136 migration rehearsal or a production-equivalent Supabase/browser test. The audit trigger applies to future F3 events; it does not retroactively attest pre-cutover rows.
- **Inherited F24:** [Hosted F24 package](evidence/M3_F3_DAYBREAK_CATALOG_V5_F24_HOSTED_REQUAL/hosted/qualify-from-00118/) remains PASS for unchanged 00118–00123 mechanics on its documented qualification fixture. It is not a clean 00001–00117 replay or M4–M15 certification. The [recovered migration fragment](recovery/recovered-migrations-incomplete.patch) is incomplete; the earlier “safely discarded” explanation remains unsupported.
- **Local gates:** `npx tsc --noEmit` PASS; targeted ESLint for the three changed TypeScript/TSX files PASS; `npm run build` PASS; `test:product-money` 30/30, `test:payment-receipt-producer` 13/13, and `test:product-p0-bulk-receipts` 9/9 PASS. The ordinary sandbox initially blocked Node's child process before those suites ran; permitted reruns produced the stated results. Existing whole-repository lint failures remain outside this focused repair and are not represented as a CI pass.

## Remaining qualification gates

1. **Representative environment and browser journeys:** The existing disposable Supabase project `jkorwnwwmdeflfntxntl` has the F3 floor but lacks `group_audit_logs` and M15 profile/request tables; it cannot validate migration `00137` or the essential authenticated M4–M15 and EN/FR mobile journeys. The exact handoff Vercel preview is READY but has no established isolated database/fictional-user binding. No hosted database write or unsafe preview mutation was performed. A full-schema isolated Supabase environment and candidate-bound preview with fictional users are needed for migration rehearsal, end-to-end recovery, audit visibility, and remaining browser acceptance.
2. **Independent review:** The actual `gpt-6-astra` delegation previously returned HTTP 403 before execution. The available delegation interface exposes no safeguard selector, and no access condition changed; there was no repeated identical attempt or simulated review. Astra's bounded independent PASS/HOLD remains pending and consumes none of the review cap.
3. **Required CI:** The candidate has no `.github` workflow files; GitHub exposed no ruleset and denied branch-protection read to the integration. Vercel reports a success status for the original handoff SHA, not a complete required-check policy or a new repair-branch product PASS. Required security/branch checks must be established from an authorized repository administration view and run against the packaged repair tip.
4. **Whole-product audit scope:** The repair prevents ordinary members from forging authoritative rows and makes new F3 event audit atomic. Existing non-financial client activity is explicitly unverified; any governance workflow whose approved contract requires atomic server audit still needs its own representative acceptance evidence. This result does not silently promote client reports to trusted evidence.

**Decision:** HOLD for release qualification. The two confirmed defects are repaired and pass the isolated focused regression, but full-schema/browser acceptance, required CI identification, and actual Astra independent review are outstanding. Jude retains sole explicit live-release authorization.
