# Candidate-bound growth, election and dues browser checkpoint — 2026-09-26

Executor: Daybreak Blue. Tested application code: `30e212a` (full hash in the tracker). Browser: localhost:3017 with `.env.development.local` verified to point only to isolated Supabase branch `nisipxbuvndobyxqqglf`; fictional Auth actors, external messaging suppressed with an intentionally invalid local service-role key. The production-bound Vercel Preview was not used for writes. This extends the hosted upgrade and Relief evidence; it does not replace its schema-only fixture limits or reopen VC-01/02.

## Before/after defects repaired

- M15 public organization route originally read a Next 16 Promise `params.slug` synchronously. A published fictional profile returned “Organisation introuvable” and the browser logged the dynamic-params error. Resolving `params` with React `use()` displayed the approved French public projection. A fictional visitor submitted a membership request and an abuse report; an admin marked the request reviewed, with **zero** memberships created. Setting the profile private and disabling requests made the public route unavailable. Hosted row counts: one reviewed request, one abuse report, zero new memberships.
- M14 public card verification route had the same Promise-params defect for `params.token`. A consented, nameless card produced “unavailable” before repair. After repair, its local French verification URL displayed “Membre vérifié”, HQ, generic “Membre”, and issue date, without a member name. Revoking the card changed the public URL to “Carte introuvable ou révoquée.” Hosted card display data contained only card type, issue date and organization name; `revoked=true`.
- A SQL `DATE` due on `2026-10-01` displayed `30/09/2026` in EDT because the formatter parsed date-only text as UTC midnight. The formatter now treats `YYYY-MM-DD` as a local calendar date; the same fictional dues type displayed `01/10/2026` after reload.
- The French election-close confirmation asserted that closing would publish results and notify everyone. The actual server transition only closes; publication is separate. The EN/FR copy now states this contract. Hosted election state after close was `closed`, `published_at=null`; the UI exposed a separate publication action. Results appeared only after explicit publication.

## Representative flows and observed data

- **M13 introduction.** A fictional active HQ member used the mounted group-switcher “Présenter un autre groupe” action to issue an opaque 30-day association link. The French landing page stated that introduction grants no membership or admin rights and led to ordinary login/signup group creation. No external share action was invoked. The member revoked the link; hosted `organization_referrals` state was `revoked`, `claimed_group_id=null`, `activated_at=null`. The 390×844 modal remained within the 390 px viewport. The server claim/activation negatives are in `scripts/test-growth-referral-activation.sql`; this browser pass did not create a new group or send a message. The landing page still forwards the revoked token to the later claim step, where the server rejects it; revocation removes attribution, not ordinary onboarding access.
- **M14 card.** The private member-card UI preview omitted name by default and required explicit consent. Issuing while the organization's public-card switch was off was denied. With that switch enabled for the fictional fixture, a consented nameless card was issued; local public verification showed only the approved minimal projection, then revocation invalidated it. No production URL was opened. The fictional group switch remains enabled on the isolated branch for further tests; the product default/denial was observed before enabling it.
- **M15 profile.** The fictional public profile exposed only approved display name, mission and website; a request entered moderation with no membership effect, abuse report was recorded, and privacy/unpublish suppressed public access. `scripts/test-growth-publication.sql` passes separately on the upgraded hosted branch. Spam/rate limits, cache propagation and full EN/FR mobile behavior remain acceptance work.
- **E-001/E-003/E-007/E-009 browser slice.** The isolated fictional HQ subscription was set to `pro` solely to expose the existing Elections UI; this is a test fixture, not billing evidence. A French poll (`1219a108-31a6-4591-b8d7-3253f59e4e81`) opened with one frozen eligible member, accepted one anonymous Yes ballot, concealed tally while open, then closed while `published_at` stayed null. An authorized separate publication exposed Yes 1/1 (100%); hosted receipt and ballot counts were one each. `scripts/test-elections-2-0.sql` passes in hosted rollback for broader role/eligibility cases. External notification dispatch was not qualified: the deliberately invalid local service-role key returned 401; the sole fictional member had all external channels disabled and in-app enabled.
- **F4-002/F4-004 browser slice.** A French one-time $5 USD dues type was due `2026-10-01`. First submission correctly failed `FUND_REQUIRED_OR_INVALID` because the fictional HQ had no unrestricted default fund; no financial effect was created. After adding a fictional unrestricted general fund, the existing pending attempt was recovered through “Récupérer cette tentative.” Exactly one payment (`02911f2a-419d-4951-817f-508544586532`) and one receipt event (`d263eaf0-335b-44f7-8d97-e050958ab574`) existed, with two postings and one private authoritative audit link. Posting was custody +5 and liability -5, no income. After adding a fictional Dues Income category, the UI recognized that credit: one linked recognition event (`95c8ae0f-66fd-4df8-9439-cbb8ce679034`), liability +5 and income -5, with one audit and no second custody effect. These test-only fund/category fixtures did not weaken permissions. The full Security Revision 2 cash-state matrix and FCG-1 shared source contract remain open.
- **Hierarchy integration.** A fictional HQ owner and branch member in separate tabs showed route-authoritative scope. Branch finance was denied, HQ retained access; switching an HQ financial draft to branch dropped the draft without an intent/event. A cross-tenant deep link was blocked. `scripts/test-hierarchy-2-0.sql` passes on the hosted branch in rollback for topology and authorization cases. Full delayed-response and mobile hierarchy acceptance remains open.
- **H-005 aggregate posting boundary (later checkpoint, code `157e9ebf0b9425797d6a3e9802814ab515312161`).** On the same isolated branch through migration `00180`, a fictional HQ owner created an aggregate child unit inside a rollback transaction. The unit had no `group_id`; `prepare_manual_financial_intent` with that unit ID as `group_id` raised `insufficient_privilege`, and the probe asserted no prepared intent or financial event. A separate read after rollback found zero test units and intents. The previously verified HQ operational-group browser receipt shows the authorized counterpart. This is hosted/server evidence; mobile aggregate selection and delayed-response UI remain open under H-008/H-010.

## Gates and limits

`npx tsc --noEmit --incremental false` passed for `30e212a`; targeted ESLint on the two routes and formatter had zero errors and one inherited `no-img-element` advisory in the public profile. `git diff --check` passed. The initial `next build` could not clear a generated `.next` cache while the dev server was running. After stopping the server and removing only the resolved workspace `.next` directory, sandboxed build compilation stopped on four Google Fonts network fetches. An authorized network build then compiled successfully, passed TypeScript, generated 45 static pages, and exited 0. Remaining build warnings were middleware deprecation, edge static generation and missing `metadataBase`; they are advisory. The hosted browser fixtures above are not production data, full notification acceptance, an exhaustive multi-role browser matrix, or a substitute for independent GPT-6 Sol review. Production remains HOLD.
