# R-012 read-only cutover inventory preparation — 2026-09-26

Executor: Daybreak Blue. Exact script commit `44c1cb877d17da1ae0080926a3d1b4a89e08074b`. `scripts/inspect-relief-legacy-cutover.sql` inventories plan owner, enrollment collecting group, receipt cash/settlement/event links, claim/payout evidence, remittance branch/owner links, native currency, source IDs and owner ledger epochs. It emits review flags for missing or mismatched ownership/currency and unresolved opening/recognition treatment; it **does not** select a financial treatment or activate a plan. The query is read only and contains no production records.

On the fresh local replay43 catalog, the query parsed against the actual candidate schema. Inserting it before the rollback of `scripts/test-relief-agency-receipts.sql` returned the fictional shared plan, enrollment, three receipt sources and two remittances; the branch receipt row exposed both branch and owner financial event IDs and no review flag for the completed pair. The fixture transaction rolled back. Ignored diagnostic log: `.vercel/relief-legacy-manifest-fixture.log`.

This is preparatory evidence only. A production-record read for the real historical Relief manifest remains a separate unanswered Jude authorization; the earlier grant covered **schema-only** extraction. No production records were read in this checkpoint. R-012 remains **confirmed missing** until real sources are classified, ambiguous rows quarantined, owner/collection/settlement/opening treatment approved, and the post-boundary path is proved without duplicate effects. The existing isolated branch has not been reset or used for this step.
