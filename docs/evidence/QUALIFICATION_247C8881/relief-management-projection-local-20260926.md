# R-010 local management projection checkpoint — 2026-09-26

Application commit `f542443e19dda4e9046b3333963b4246cf8f9f4d` on `codex/qualification-repair-247c8881`. Executor: Daybreak Blue. No independent reviewer has reviewed this candidate. Production was read only and no hosted branch was changed in this checkpoint.

Fresh isolated local PostgreSQL 17.11 UTF-8 database `villageclaq_upgrade_replay42` at `localhost:55432` imported the previously preserved production S0/M2 **schema-only** baseline (SHA-256 `3b539557f807efca02b92fde9307228c45a6b398abc60a96e000150b1d1bdd4a`), filtered local import (`63983281e445e84aa14f0086b1696168144967bcbcc283b0c8afd137e5d1af9e`), synthetic platform fixture (`016170b2147631a4ca391a140ad6ff4ea96db12297668d6c54a65e4306e9151f`) and the documented local auth-trigger postlude. The filter omits `supabase_admin` default-privilege statements that the temporary login cannot alter. All **53** repository migration files `001170`–`00166` applied in filename order with `ON_ERROR_STOP=1`; names are recorded in ignored `.vercel/local-replay42-apply.names`. Direct SQL did **not** record Supabase migration history. It is compatibility and behavior evidence, not a hosted upgrade or production-equivalent restore.

The fictional, rollback-only `scripts/test-relief-agency-receipts.sql` exited 0 on replay42 (`.vercel/local-replay42-test-relief-agency-r010.log`). It retained branch receipt, owner recognition, maker/checker remittance and audit/retry probes. The new management assertions observed branch due 20, owner receivable 20, internal elimination 20, both net zero; an additional unmatched agency receipt yielded branch due 30, owner receivable 20, elimination 20 and residual branch due 10. Local financial-event count was unchanged by the projection. A pre-event as-of returned no nonzero balance; EUR showed none for USD plan. A branch actor and cross-tenant actor were denied; a stale topology returned no projection. Effective RPC EXECUTE: `anon=false`, `authenticated=true`, with server current-scope checks.

The HQ Relief rollup caller uses current organization/group reporting unit, currency and snapshot time; the RPC checks frozen reporting audience plus current `reports.view`. The page identifies the projection as read only and preserves branch/local reporting separately. Added missing EN/FR keys used by this mounted page. `npx tsc --noEmit`, targeted `npx eslint --quiet` and `git diff --check` passed at the code commit. Browser rendering and hosted effective grants remain untested. R-006 delegated payout postings and R-012 legacy classification are not yet sources in this projection; it must be rechecked when those paths change. VC-01/02 closure in their previous tested financial scope is unchanged.
